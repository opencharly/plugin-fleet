# 2026.250.0558 — chore: rename the four remaining fleet-named files (R5 filename sweep, CC-2 follow-up)

## Summary

R5 filename-sweep follow-up to plugin-fleet #15 (the CC-2 vocabulary retirement). The #15 content sweep passed, but the R5 filename sweep (`git ls-files | grep -i fleet`) still matched four basenames. Renamed:
- `fleet_cmd.go` → `deploy_cmd.go`
- `fleet_del_argv_test.go` → `deploy_del_argv_test.go`
- `fleet_test_helpers_test.go` → `deploy_test_helpers_test.go`
- `node_fleet_venue_test.go` → `node_deploy_venue_test.go`

## How tested

```
$ git ls-files | grep -iE "/[^/]*fleet[^/]*$" | grep -v CHANGELOG | wc -l
0

$ go build ./... && go test -count=1 ./...   # all green
```

## Rulebook compliance

- **R5**: the filename sweep is now zero (the four basenames renamed in this PR).
- **R6**: PR-only landing, no force-push.
- **R7**: gates proven above.

## Change classification

- **Change class**: non-breaking filename rename (R5 sweep completion).
- **Verification gate**: analysed on a live system — build + test green on the final tree; no runtime behavior change (pure file renames).
- **Attribution tier**: analysed on a live system.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*