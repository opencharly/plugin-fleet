# 2026.250.0829 — chore(cc2): align to the charly #554 contract pins — sdk v0.2026250.758 + spec v0.2026250.709

## Summary

CC-2 wave completion for the plugin-fleet command provider. The command provider
migration itself (command:fleet → command:deploy, FleetCmd → DeployCmd, FleetNode →
DeployNode, fleetDiscForEntity → deployDiscForEntity) landed in #15, pinned to the
wave's in-flight sdk v0.2026250.527 / spec v0.2026250.523. charly #554 (tag
v2026.250.0820) then landed the FINAL wave pins and retired the `fleet` word with a
load-time hard error (exit 80). This PR completes plugin-fleet's alignment to that
final contract:

- **Re-pin** sdk v0.2026250.527 → **v0.2026250.758** (NEWEST tag — the sdk #234
  duplicate-top-level-key fix charly #554 consumes) and spec v0.2026250.523 →
  **v0.2026250.709** (sdk 758's transitive require); `go mod tidy` drops the stale
  .527/.523 sum entries.
- **R5 sweep completion**: the final non-repo-name `fleet` reference — the
  host_infra_test.go comment naming the sdk #227 self-deadlock fix — is reworded to
  the current command word (`deploy-del`; sdk #227 anchor kept).

## How tested

1. `go build ./... && go vet ./... && go test ./... -count=1` — GREEN
   (`ok github.com/opencharly/plugin-fleet/candy/plugin-fleet 0.546s`).
2. Claim-keyed sweep (executed on the final tree):
   `git grep -iw fleet -- ':!CHANGELOG'` → 18 matches, EVERY one a plugin-fleet
   repo-name / module-path / candy-name reference (README.md, candy/plugin-fleet/
   charly.yml, go.mod module path, and comments naming the repo) — the R5 sweep
   disposition is the repo name itself. Zero command:fleet / FleetCmd / FleetNode /
   FleetConfig / fleetDisc / acc.Fleet / `fleet_cmd` matches remain.
3. Live smoke — charly rebuilt from the MERGED origin/main tree with THIS module
   substituted (temp `go.mod` `replace` + `go mod tidy`, build exit 0):
   - `charly deploy --help` → exit 0, serves the deploy grammar
     (`deploy add <name> [<ref>]` shown in `deploy add --help`, exit 0).
   - `charly fleet` → exit 80 with the charly hard error:
     `charly: error: the "fleet" command is retired in the CC-2 vocabulary cutover — use `charly deploy add` / `charly deploy del` instead (fleet → deploy).`

## Rulebook compliance

- **R0** — git-workflow + plugin skills loaded before any repo command; charly/AGENTS.md read.
- **R1** — no warnings/failures across build/vet/test; the one sweep match beyond the repo
  name (the sdk #227 comment) got the comment reworded, not dispositioned-as-out-of-scope.
- **R2** — the whole remaining fleet-vocabulary sweep finished in this PR: zero
  non-repo-name `fleet` matches after the final tree.
- **R3** — no duplication: only pins + one comment line changed; no new mechanisms.
- **R4** — no workarounds; the smoke used the real merged charly main + real module substitution.
- **R5** — legacy removed completely: the old command word dies at the charly load-time
  hard error (exit 80), proven live; the last `fleet`-named comment reference is reworded.
- **R6** — no force-push, no main push: feat/cc2-deploy-word pushed once, fast-forward only.
- **R7** — build/vet/test are the repo's gate; pasted above (there is no task verify at this level).
- **R10** — verified from the final committed tree; evidence captured above.

## Change classification

- **Change class**: dependency re-pin + comment sweep completion (breaking pin floor).
- **Verification gate**: go build/vet/test green + live CLI smoke (served word works,
  retired word hard-errors at exit 80).
- **Attribution tier**: fully tested and validated — gates ran on the final committed
  tree, the changed paths (go.mod pin + host_infra_test.go) executed live, and both
  sides of the command-word contract (new serves, old errors) were proven on a charly
  binary built against this exact module.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*