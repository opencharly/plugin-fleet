# 2026.249.2126 — refactor!: drop the group-kind IsGroup dependency (spec #105)

## Summary

Migrates plugin-fleet to spec v0.2026249.2106 (the `group:`-kind removal, spec #105) + sdk v0.2026249.2114 (the deploykit group-root self-skip deletion, sdk #224). Follow-up to this repo's own member-tree migration (#13, tag v2026.249.2010), which predated the `Deploy.IsGroup()` deletion and did not compile against 2106.

Changes:
- **go.mod/go.sum**: spec v0.2026249.1948 → **v0.2026249.2106**, sdk v0.2026249.1959 → **v0.2026249.2114**; `go mod tidy` clean.
- **deploy_target.go (arbiterBracketAcquire)**: the `IsGroup: node.IsGroup()` projection into `spec.ArbiterInvokeInput` is DELETED as a dead arm (field left Go-zero). RCA: a fleet lifecycle claimant is always a primary substrate node post-migrate — the former targetless group shape cannot be authored (the kind is removed; `charly migrate` folds group scalars onto the primary substrate; the loader rejects a kindless root), so the arm guarded no live behavior. Positional re-derivation (`Target == "" && HasMembers()`) would be equally dead. Byte-for-byte peer disposition: candy/plugin-check/bed_session.go `arbiterAcquire` (df95f80) deleted the identical projection with the same reasoning. The `ArbiterInvokeInput.IsGroup` wire FIELD remains a spec#105 residual consumed by plugin-preempt; plugin-fleet no longer ever sets it non-zero.
- **bed_persist_writer_test.go**: `TestPersistBedDeployOverrides_GroupBedNotPersisted` DELETED (R1 RCA first): it pinned the sdk-side group-root self-skip guard that sdk #224 deliberately deleted in this same wave — it fed a programmatically-constructed unauthorable shape (targetless member-bearing root) and asserted the deleted dead-code skip. `TestOverlayRoundTrip_GroupMembersSurvive` KEPT: it pins still-live sdk 2114 node-form behavior (`fleetDiscForEntity` still classifies a targetless member-only STATE entry as `"group"` in the per-host overlay serializer) and does not touch the deleted spec method. The ephemeral.go/ephemeral_test.go `#GroupInput` comment mentions are historical RCA references and stay unchanged.

## How tested

```
$ go build ./... && go vet ./... && echo BUILD_VET_OK
BUILD_VET_OK

$ go test ./... -count=1; echo TEST_EXIT=$?
TEST_EXIT=0
ok  	github.com/opencharly/plugin-fleet/candy/plugin-fleet	0.491s
?  	github.com/opencharly/plugin-fleet/candy/plugin-fleet/cmd/serve	[no test files]
```

Executed sweep on the final committed tree (B14(d)):

```
$ git grep -n "IsGroup" -- "*.go"          → candy/plugin-fleet/deploy_target.go:880 (explanatory comment only)
$ git grep -n "node.IsGroup\|\.IsGroup()" -- "*.go"  → zero call sites
$ git grep -n "kind: *group\|^group:" -- "*.go" "*.yml" "*.cue" "*.md" → zero matches
$ git grep -n "GroupInput"                 → 2 historical RCA comment references (ephemeral.go:408, ephemeral_test.go:17), no live dependency
```

Intermediate failure captured and RCA'd (not retried blind): the first `go test -count=1` run failed exactly one test (`TestPersistBedDeployOverrides_GroupBedNotPersisted`) — root cause: the sdk-side guard it pinned was deleted by sdk #224 in this same wave; dispositioned by test deletion per R5, after which the full gate passed.

## Rulebook compliance

- **R0 (skills first)**: git-workflow + root-cause-analyzer + strict-policy disciplines applied (R1 RCA executed on the test failure BEFORE remediation; disposition documented above).
- **R1 (RCA every anomaly)**: the single test failure was root-caused to the sdk #224 guard deletion (verified against the pinned sdk source, deploykit/bed_session.go — only `HostRooted || externalInPlace` remains) before any edit; no blind retry.
- **R2 (narrow scope)**: only the compile-blocking site + the test pinning deleted sdk behavior changed; no drive-by refactors.
- **R3 (no duplication)**: the arbiter projection disposition mirrors the peer plugin-check implementation rather than inventing a second spelling; no new helpers added.
- **R4 (no workarounds)**: no sleeps, retries, or pins hand-edited — pins moved via `go get` + `go mod tidy`.
- **R5 (delete legacy completely)**: the dead IsGroup arm and its dead test are deleted outright, not commented out; remaining "group" words are live sdk node-form classification or historical RCA references, each dispositioned above.
- **R6 (git safety)**: feature branch only, no force-push, no hook bypass, no direct main push.
- **R7 (prove the gate)**: build/vet/test -count=1 output pasted above, run on the committed tree.
- **R8/R9**: PR-only landing via the org workflow; validator governs the merge.
- **R10 (fresh disposable proof)**: gates executed after the final commit on the final tree (`git status` clean at gate time).
- **PR body IS the changelog** (tag-on-merge writes CHANGELOG/<calver>.md at merge).

## Change classification

- **Change class**: refactoring (consumer cutover pin-bump + dead-arm deletion; one test deleted per R5 with RCA). No runtime behavior change on any authorable config.
- **Verification gate**: `go build ./... && go vet ./... && go test ./... -count=1` on the final tree; executed identifier sweep pasted.
- **Attribution tier**: *fully tested and validated* — full module gate green on the final committed tree; changed paths (arbiterBracketAcquire compile site, bed_persist_writer_test suite) executed live by the gate.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*