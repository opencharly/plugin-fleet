# 2026.249.2010 — feat!: migrate plugin-fleet to the Deploy member-tree IR (spec #103 + sdk #221)

## Summary

Cutover C consumer migration: plugin-fleet moves onto the ONE ordered Deploy member tree (spec #103 + sdk #221). The dual `Children`/`Members` maps and their helper surface are DEAD in spec v0.2026249.1948; every consult site now consumes the position-derived helpers (`InSubstrateMembers()` / `DeployLevelMembers()` / `MemberByName()` / `HasMembers()`) — never a reconstructed dual map.

- **Pins**: spec `v0.2026246.554` → `v0.2026249.1948`, sdk `v0.2026246.640` → `v0.2026249.1959` (`go mod tidy` clean).
- **walk.go**: pre-order walk iterates `node.InSubstrateMembers()` in authored order; `sortedChildKeys` (the map-key sort) is deleted — the tree carries authored order.
- **members_persist.go**: persists `root.DeployLevelMembers()` (position-derived successor of the former `Members` map iteration over `SortedMemberKeys`); no key sort.
- **del_resolve.go**: dotted-path resolution walks `cur.MemberByName(seg).Node` — mirrors the canonical `spec/fleet.ResolveNodePath` pattern.
- **Tests migrated to the Member tree**: `deploy_node_test.go` (makeTree + new `TestMemberTree_AuthoredOrder` pinning the ordered-iteration contract, replacing the deleted `SortedNestedKeys` test), `ephemeral_test.go` (`HasMembers()` structure-not-copied assertion), `deploy_nested_pod_test.go` (`MemberByName` lookup), `bed_persist_writer_test.go` (decode helper appends positioned `Member` entries; round-trip tests via `MemberByName`; `childTestKeysOf` → `memberTestNamesOf`), `bed_check_live_refs_test.go` (parent-node fixtures with in-substrate members), `deploy_chain_test.go` + `node_fleet_venue_test.go` (member-tree fixtures), and the unknown-child error hint migrated to the new "available members" wording.
- **BREAKING** (in-repo contract): consumers must pin spec ≥ v0.2026249.1948.

## How tested

```
$ go build ./... && echo BUILD_OK
BUILD_OK

$ go vet ./... && echo VET_OK
VET_OK

$ go test ./... -count=1 2>&1 | tail -4
ok  	github.com/opencharly/plugin-fleet/candy/plugin-fleet	0.471s
?   	github.com/opencharly/plugin-fleet/candy/plugin-fleet/cmd/serve	[no test files]
```

Executed sweep (B14(d)) — every removed identifier, real `git grep` output:

```
$ git grep -nE "SortedMemberKeys" -- '*.go'          → ZERO MATCHES
$ git grep -nE "SortedNestedKeys" -- '*.go'          → ZERO MATCHES
$ git grep -nE "ValidateDeploymentChildren" -- '*.go' → ZERO MATCHES
$ git grep -nE "HasChildren" -- '*.go'               → ZERO MATCHES
$ git grep -nE "\.Inside\(" -- '*.go'                → ZERO MATCHES
$ git grep -nE "sortedChildKeys" -- '*.go'           → ZERO MATCHES
$ git grep -nE "childTestKeysOf" -- '*.go'           → ZERO MATCHES
$ git grep -nE "\.Children\b" -- '*.go'              → ZERO MATCHES
$ git grep -nE "\.Members\b" -- '*.go'               → ZERO MATCHES
```

## Rulebook compliance

- **R0 (skills first)**: umbrella rulebook loaded via project context; charly-internals git-workflow doctrine (feat/ branch, PR-only, R10, no force-push) followed.
- **R1 (RCA)**: every compile/vet/test failure was RCA'd before remediation (e.g. the `BedCheckLiveRefs` signature change and the sdk error-wording cutover were traced to the sdk #221 surface before migrating the tests).
- **R2 (spec-first)**: implementation consumes the spec #103 member-tree contract (`spec/member_tree.go`) exactly; no local re-derivation of position.
- **R3 (no duplication)**: no dual-map reconstruction anywhere; iteration derives from position helpers only; dotted-path walk mirrors `spec.ResolveNodePath` instead of duplicating a second walker semantics.
- **R4 (no workarounds)**: no sleeps, retries, or hand-pins; the ordering guarantee comes from the authored-order Member list, not a sort.
- **R5 (delete legacy completely)**: `sortedChildKeys`, `childTestKeysOf`, and every dead-identifier usage deleted in this PR — no aliases, no shims.
- **R6 (git safety)**: no force-push, no direct main push, no hook bypass; `git status` clean before commit; submodule git ops via `git -C <abs-path>` from the umbrella root.
- **R7 (prove the gate)**: `go build`/`go vet`/`go test -count=1` pasted above on the final committed tree.
- **R8 (scope)**: only plugin-fleet's own module touched; no edits to spec/sdk/other repos.
- **R9 (attribution)**: footer below in the exact required form.
- **R10 (fresh disposable proof)**: gates run on the final committed tree (working tree clean at commit f49e4ac before the run tails above were captured).

## Change classification

- **Change class**: breaking consumer migration (code + unit tests + go.mod pin bump) in a standalone plugin repo.
- **Verification gate**: local Go gates (`go build ./...`, `go vet ./...`, `go test ./... -count=1`) — this repo has no pinning gate; the org `pr-validator` judges at merge.
- **Attribution tier**: fully tested and validated (gates passed on the final tree; all changed paths compiled and executed under `go test`).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*