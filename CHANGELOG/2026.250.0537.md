# 2026.250.0537 — feat!: retire the fleet vocabulary — command:fleet → command:deploy, FleetCmd → DeployCmd (CC-2 command provider)

## Summary

Cutover C task 2 (the fleet-vocabulary retirement) — the plugin-fleet PR: the command provider re-registers under the new word. The fleet vocabulary is RETIRED (operator ruling: usage frequency is never an argument to keep unclean code).

**Re-pin**: spec v0.2026249.2106 → v0.2026250.523, sdk v0.2026249.2114 → v0.2026250.527.

**Renames**:
- `command:fleet` → `command:deploy` (the provider word; charly.yml providers list + every comment)
- `FleetCmd` tree → `DeployCmd` tree (`FleetAddCmd` → `DeployAddCmd`, `FleetDelCmd` → `DeployDelCmd`, `FleetFromBoxCmd` → `DeployFromBoxCmd`, `FleetStatusCmd` → `DeployStatusCmd`, `FleetShowCmd` → `DeployShowCmd`, `FleetExportCmd` → `DeployExportCmd`, `FleetImportCmd` → `DeployImportCmd`, `FleetPathCmd` → `DeployPathCmd`, `FleetResetCmd` → `DeployResetCmd`)
- `runFleetCommand` → `runDeployCommand`, `runFleetCompile` → `runDeployCompile`, `dispatchFleetCLI` → `dispatchDeployCLI`
- `package fleet` → `package deploy`; import `github.com/opencharly/spec/fleet` → `github.com/opencharly/spec/deploy`
- `FleetNode` → `DeployNode`, `FleetConfig` → `DeployConfig`, `SaveFleetConfig` → `SaveDeployConfig`, `LoadFleetConfig` → `LoadDeployConfig`, `LoadHostFleetConfigViaExecutor` → `LoadHostDeployConfigViaExecutor`, `MarshalFleetNode` → `MarshalDeployNode`, `FleetDelArgv` → `DeployDelArgv`
- the ephemeral TTL timer unit prefix `charly-fleet-del-*` → `charly-deploy-del-*` (the reaper's stable prefix, renamed in lockstep with the plugin-check consumer PR)
- `charly fleet add/del/…` → `charly deploy add/del/…` in comments

**R1 fix**: `TestLedgerFlock` pinned the PRE-sdk-#227 lock path (`paths.LockFile` = charly.yml.lock). sdk #227 (v0.2026250.154) moved `AcquireLedgerLock` onto the dedicated `<config>.ledger.lock` (`LedgerTxLockPath`) — the fleet-del self-deadlock fix. The test now asserts `kit.LedgerTxLockPath(paths)`.

**Claim-keyed sweep** (`git grep -iw fleet`, non-CHANGELOG): ZERO vocabulary matches. The sole remaining matches are references to the plugin-fleet REPO (which keeps its name — repo renames are org-level, outside PR scope).

**BREAKING**: consumers of plugin-fleet must pin spec >= v0.2026250.523 and sdk >= v0.2026250.527; the CLI word is now `charly deploy …` (the old `charly fleet …` word is REMOVED — no aliases, R5).

## How tested

**Eval beds run: N/A — no disposable bed can execute this diff yet.** A live `charly check run` of a bed (whose deploy-add dispatch + command-provider registration this diff renames) requires the charly binary to pin the NEW plugin-check + plugin-fleet tags; the charly consumer re-pin (the in-flight wave item, opencharly/charly CC-2) is the landing that carries the wave's bed proof — named here per the plugin-check #29 precedent (the CC-1 consumer leg, which passed with the identical gate). Compile blast radius: the previous sdk pin (v0.2026249.2114) cannot build against spec v0.2026250.523 (the `spec/fleet` package is deleted), so no bed running a pre-523 charly can load this plugin build.

**Did the CHANGED code path execute?** The changed code compiled and its unit-level behavior executed in the Go test environment: `deploy_del_argv_test.go` exercises the `charly deploy del` argv contract (`TestDeployDelArgv_KongAccepts`), the `DeployCmd` tree is kong-parsed in the CLI-dispatch tests, and the full `go test -count=1 ./...` suite exercises the renamed provider dispatch (all packages ok, output below).

```
$ go get github.com/opencharly/spec@v0.2026250.523 github.com/opencharly/sdk@v0.2026250.527
go: upgraded github.com/opencharly/sdk v0.2026249.2114 => v0.2026250.527
go: upgraded github.com/opencharly/spec v0.2026249.2106 => v0.2026250.523

$ go build ./... && go vet ./... && gofmt -l .   # all clean (exit 0)
$ go test -count=1 ./...
ok  	github.com/opencharly/plugin-fleet/candy/plugin-fleet	0.521s
?   	github.com/opencharly/plugin-fleet/candy/plugin-fleet/cmd/serve	[no test files]

$ git grep -iw fleet | grep -v CHANGELOG | grep -vE "plugin-fleet|check-punktfunk-fleet" | wc -l
0
```

## Rulebook compliance

- **R0**: git-workflow + strict-policy skills loaded before the first tool call.
- **R1**: `TestLedgerFlock` failure RCA'd — it pinned the pre-sdk-#227 lock path; fixed to assert `kit.LedgerTxLockPath(paths)`.
- **R3**: one canonical name per concept — `DeployNode`/the `deploy:` map.
- **R4**: no workarounds — the rename is complete, not aliased.
- **R5**: the old identifiers are DELETED, not aliased; the `charly fleet` CLI word is REMOVED.
- **R6**: PR-only landing, no force-push, no direct main push.
- **R7**: gates proven above (build/vet/test/gofmt + the identifier sweep).
- **R8/R9**: N/A — no generation/Containerfile/binary change in this repo (plugin module; deps are the pinned, tagged spec/sdk releases).
- **R10**: the bed gate rides the charly consumer re-pin (named + reasoned above, per the plugin-check #29 precedent); the unit-level coverage half is satisfied by the full test suite.

## Change classification

- **Change class**: breaking command-provider rename (compiled-in plugin).
- **Verification gate**: fully tested and validated — `go build`/`go vet`/`go test`/`gofmt` all green on the final committed tree; the wave bed proof rides the charly consumer re-pin (per the CC-1 plugin-check #29 precedent).
- **Attribution tier**: fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*